FIX: The quantization formula in the summary contains a typo: the set of possible quantized values is incorrectly written as `\{\frac{1}{2^b - 1}, \iota \in 0, \dots, 2^b - 1\}`. It should be `\{\frac{\iota}{2^b - 1} \mid \iota \in 0, \dots, 2^b - 1\}` to correctly represent the constrained uniform quantization levels. Additionally, the "Limitations & Constraints" section omits a critical trade-off explicitly stated in the paper: the method incurs **additional computational overhead on the clients** due to the local distillation step, which can be challenging in environments with limited client resources or high client counts. This limitation should be added to accurately reflect the paper's own discussion.