FIX: The draft summary incorrectly states that "the specialist model training is **not parallelizable** in the same way as standard ensemble training (requires a generalist first)." In the original paper, Section 5.2 explicitly states that "specialist models can be trained rapidly and in parallel" and Section 5.5 notes that "all the specialists are trained completely independently." While a generalist model is required first to define the clusters, the actual training of the specialists is highly parallelizable. Please correct this statement in the "Key Limitations" bullet to reflect that specialist training is parallelizable (e.g., "Specialist training requires a pre-trained generalist but then proceeds in parallel").