FIX: The draft summary is incomplete (cut off mid-sentence) and contains an inaccurate formula not present in the original paper. The quantization equation \(Q(x) = \text{round}(x/\Delta) \cdot \Delta\) with \(\Delta = \frac{\max(x) - \dots\) is not provided or referenced in the survey. The paper does not include explicit quantization formulas; it only describes the two broad approaches (post-training quantization and quantization-aware training) without mathematical expressions. The summary must be corrected by removing this invented formula. Additionally, the summary should be completed, incorporating the actual formulas found in the paper for performance evaluation metrics (latency \(L = t_{\text{end}} - t_{\text{start}}\), throughput \(T = N_{\text{operations}} / t_{\text{elapsed}}\), energy per inference \(E_{\text{inference}} = P_{\text{avg}} \times t_{\text{inference}} / N_{\text{inferences}}\), memory utilization, accuracy/precision/recall/F1, and availability) rather than misrepresenting the quantization technique. The summary also lacks coverage of the federated learning formulations and the full content of Section 2.5, which contains the correct mathematical expressions. The truncated text must be appended to provide a complete and accurate summary aligned with the paper's content.